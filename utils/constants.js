const noUser = 'User does not exist';
const emailUsed = 'That email address is already in use';
const invalidEmailPassword = 'Invalid email or password';
const incorrectEmailPassword = 'Email or password incorrect';
const articleNotFound = 'No articles found';
const cardExists = 'Card already exists';
const denyDelete = 'You cannot delete that';
const authRequired = 'Authorization required';
const authFailed = 'Authorization verification failed';
const serverError = 'An error occured on the server';

module.exports = {
  noUser,
  emailUsed,
  invalidEmailPassword,
  incorrectEmailPassword,
  articleNotFound,
  cardExists,
  denyDelete,
  authRequired,
  authFailed,
  serverError,
};
